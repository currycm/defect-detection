# ============================================================
# 仅推理服务（部署/Docker 用）—— 使用 ONNXRuntime CPU，不装 torch。
# 与开发机全量依赖（requirements.txt）分开，镜像体积 ~1.5GB -> ~400MB。
# ============================================================
onnxruntime>=1.16.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
python-multipart>=0.0.9
numpy>=1.24.0
pyyaml>=6.0
# headless 版：容器无 GUI，避免拉入 libGL 等系统依赖
opencv-python-headless>=4.8.0
pillow>=10.0.0
